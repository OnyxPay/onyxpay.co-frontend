------------
придумать как слушать ивенты с блокчейна, например после создание Deposit заявки
дождаться следующего блока и получить ивент.
----------

Deposit:
agent => super agent:
* форма
  agentAN => publick key
  amount
* получить список cупер агентов доступных в той же стране (из БД) и с 
  нужным количеством ассетов (информация хранится в блокчейне)
* Таблица доступных агентов
  Должна быть доступна функциональность выбора агента, которому отправляется заявка.
* Cтраница с паролем, для декодирования pk.
* Вызвать смартконтракт для создания заявки. 



Role-Based Access Control (RBAC):
* разрешить некоторые роуты для определынных ролей (guard routes) +
* скрывать или показывать части UI ?

Permissions:
Request:
  view, edit, delete

Если пользователь не залогинен, делать редирект на страничку логина
  !Если форма логина будет на другом поддомене, придется костылить редирект "react-router-dom"
  logout: удалить куку, редирект на страницу логина

Если залогинен проверить роль пользователя или permission
  allow or redirect 